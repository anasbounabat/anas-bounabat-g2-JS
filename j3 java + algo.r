let a= "jean"
let b= "paul"
let resu"lt = (a = b)

function checkanme (prenom1 , prenom2)
{
if (prenom1 === prenom2) 
{
return "les 2 prenoms sont identiques"
} else {
    return "les 2 prenoms ne sont pas identiques "
}

}
console.log(resuult)


let names = ["Vincent","Paul","Arthur]

names.forEach(fonction(names)
{

console.log(ajouter "va a la peche")
}
(

))

let student = 
{
name:'Nicolas'
favoriteFood : 'Pizza'
city :' Paris'

if (15/2)=2
{
Si ce nombre est pair, afficher dans la console "pair"
}
Else (15/2)=1
{
Si ce nombre est impair, afficher dans la console "impair"
}'
}