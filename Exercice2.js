classTES
 {
    constructor(name , attack , defense, hp )
 }
       this.name = Jason
       this.attack = attack
       this.defense = defense
       this.hp = 100

  
  getattack(Jason tue un survivant)
  {
  let array.splice(survivant)
  }
  While(hp>0)
  {
  degats = this.attack - 60
  }
  
  
  
  
  let survivant = ["mickael","Trevor","Franklin","Lester","Lamar" ]
  60 this.hp
  
  
  Math.Random
  
  If ((maths.random = 0.3 survivant.attack) 
  {
  let damage = hp Jason - 10 array.splice(survivant)
  }
  Consol.log()
  
  If (maths random = 0.5 survivant die )
  {let damage = hp survivant = 0}
  
  If (maths random = 0.7 survivant die )
  {
  
      let damage hp Jason - 15 && hp survivant = 0  array.splice(survivant)
  }
  
 if (survivant hp =0)
 {
 
  let names = ["mickael","Trevor","Franklin","Lester","Lamar"]

names.forEach(fonction(names)}
{

console.log(ajouter "le survivant est mort")
  
  